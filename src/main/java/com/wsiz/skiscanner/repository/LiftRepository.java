package com.wsiz.skiscanner.repository;

import com.wsiz.skiscanner.model.Lift;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LiftRepository extends JpaRepository<Lift, Long> {
}
